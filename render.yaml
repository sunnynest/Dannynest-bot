services:
  - type: web
    name: sunnynest-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    pythonVersion: 3.11.8
fix: specify correct Python version for Render
